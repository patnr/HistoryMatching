# Top-level (direct) requirements
# This list is manually maintained, whereas
# requirements.txt is generated by pip freeze.
# The dependencies are usually "abstract", but can also be pinned.

jupyter
jupytext
# Use Colab's mpl version
matplotlib
multiprocessing-on-dill==3.5.0a4
scipy
tqdm

# By Patrick
mpl-tools==0.2.39
patlib==0.3.5
struct-tools==0.2.5
