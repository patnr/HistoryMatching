# NB: pre-history-rewrite hash -- re-pin once MiniRes' history is rewritten.
git+https://github.com/patnr/MiniRes.git@1b9f8d56010a
# "Editable" mode:
# -e ${HOME}/path/MiniRes

jupyter
pathos
threadpoolctl
adjustText==0.8
# NB: 0.4.0 uses `mpl.rcsetup.interactive_bk`, removed in matplotlib 3.11
# (and 0.4.1 has a `freshfig` bug) -- so this floor is load-bearing.
mpl-tools>=0.4.2,<0.5
# Used by the notebooks; used to arrive transitively, via TPFA-ResSim.
struct-tools
